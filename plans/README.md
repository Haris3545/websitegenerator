# Animation & design improvement plans

Generated by an `improve-animations`-style audit (emil-design-eng + apple-design philosophy, `find-animation-opportunities`/`improve-animations` methodology) against commit `46a1b7b`, covering the whole websitegenerator app — both the generated artist "site" and the internal "builder" admin tool. 16 corrective findings + 4 additive "missed opportunity" suggestions, each written as a fully self-contained plan for any executor (agent or human) with zero other context.

## Plans table

| # | Title | Severity | Category | Status | Depends on |
|---|---|---|---|---|---|
| 001 | Introduce shared easing tokens | HIGH | Cohesion & tokens | TODO | — |
| 002 | Fix ease-in on exit keyframes | HIGH | Easing & duration | TODO | 001 |
| 003 | Fix scale(0) entrances | HIGH | Physicality & origin | TODO | — |
| 004 | Interruptible modal close | HIGH | Interruptibility | TODO | 001 (for `--ease-out` in the transition rule) |
| 005 | AestheticPanel CSS-var thrash | HIGH | Performance | TODO | — |
| 006 | Drag animations use transform | HIGH | Performance | TODO | 001 |
| 007 | Reduce tab-switch animation frequency | HIGH | Purpose & frequency | TODO | 001 |
| 008 | Add :active press feedback (14 sites) | MEDIUM | Physicality & origin | TODO | — |
| 009 | Anchor popover transform-origin (5 sites) | MEDIUM | Physicality & origin | TODO | — |
| 010 | Reduced-motion + hover gating (repo-wide) | MEDIUM | Accessibility | TODO | — |
| 011 | Drag boundary rubber-banding (3 components) | MEDIUM | Interruptibility | TODO | — |
| 012 | SwipeCard JS/CSS duration mismatch | MEDIUM | Interruptibility | TODO | — |
| 013 | IdeaFolderView velocity-based commit | MEDIUM | Interruptibility | TODO | — |
| 014 | Scope transition-all (24 files) | LOW | Performance | TODO | — |
| 015 | Add entrance stagger (8 surfaces) | LOW | Cohesion & tokens | TODO | 001 |
| 016 | Fix poof-particle duration drift | LOW | Cohesion & tokens | TODO | — |
| 017 | ProvisioningOverlay completion reveal | MEDIUM | Missed opportunities | TODO | 001, 015 (optional reuse) |
| 018 | ArtistForm "Live." deploy reveal | LOW | Missed opportunities | TODO | 001 (optional) |
| 019 | SwipeStack empty-state entrance | LOW | Missed opportunities | TODO | 001 (optional) |
| 020 | ConversationThemes accordion reveal | MEDIUM | Missed opportunities | TODO | — |

## Recommended execution order

1. **001** first — it's the token foundation nearly every other plan's CSS references (`var(--ease-out)`). Land it before anything else.
2. **002, 003** — small, isolated, highest-severity CSS-only fixes. Safe to do immediately after 001, in either order.
3. **004, 005, 006, 007** — the remaining HIGH-severity items. Independent of each other; can be done in any order or in parallel by different executors, since they touch entirely different files (`useClosableOverlay.ts`/`globals.css`; `AestheticPanel.tsx`; `CampaignGanttBoard.tsx`/`CampaignTimeline.tsx`/`ArtistsBoard.tsx`; `globals.css`/`NavPills.tsx`).
4. **008–013** (MEDIUM) — all independent of each other. 008 and 009 are the most mechanical (bulk className edits); do those first for quick wins.
5. **014–016** (LOW) — mechanical/polish. 015 depends on 001; 014 and 016 don't.
6. **017–020** (missed opportunities, additive) — do last, after the corrective work has landed, since 017–019 optionally reuse plan 015's `.animate-list-item-in` utility if it exists (each plan has a fallback if it doesn't).

## Notes for whoever executes these

- Every plan was written against commit `46a1b7b` — if the codebase has moved on significantly by execution time, expect some plans' cited line numbers to have drifted; each plan's own Boundaries section says to stop and report rather than improvise past that.
- Plans 008 and 014 are the largest in file-count (12 and 24 files respectively) but each is a single, repeated, mechanical edit — good candidates for a less capable/cheaper executor model.
- Plans 004, 005, 006, and 010 involve real behavioral logic changes (not just CSS/className edits) and deserve a more careful executor and reviewer.
- No plan here adds a new dependency (no motion library) — everything stays consistent with this repo's existing hand-rolled-CSS-and-Pointer-Events approach.
